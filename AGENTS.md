# Repository guidance

Before reading implementation files, search `vault.md` for the relevant component and follow its pointers into `docs/`. After changing a component, update its vault entry, invariants, test map, and change log in the same change.

Do not commit generated virtual environments, caches, temporary databases, or local plan files.

